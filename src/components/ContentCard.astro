---
import{sectionBySlug}from"../config";const{entry}=Astro.props;const section=sectionBySlug.get(entry.data.section);
---
<a class="content-card" href={`/${entry.data.section}/${entry.id}/`}><span class="meta">{section?.label}</span><h3>{entry.data.title}</h3><p>{entry.data.description}</p></a>
